

# 网络基础知识-IP
; IP地址可以理解为就是设备在互联网中的地址(唯一身份证)，每一个连入网络的设备都有一个自己的IP地址，用来定位设备在互联网中的位置。
; IPv4地址:32位二进制
; IPv6地址:128位二进制

; 注意:127.0.0.1是一个非常特殊的IP地址，表示的是本机地址(也称为本地回环地址)

# 网络基础知识-端口
; 端口是网络中设备上用于标识进程的数字，范围是0-65535，其中0-1024是预留给系统进程的，1024-65535是用户进程使用的。
; 端口号可以理解为设备上运行的进程的标识，一个设备上可以运行多个进程，每个进程可以监听一个端口，这样就可以通过端口来区分不同的进程了。

; 网络基础知识-域名
; 域名(DomainName)，是由一串用点分隔的英文字母组成的，例如：www.aliyun.com
; IP地址不便于记忆，因此设计出了域名，并通过DNS(Domain Name System，域名解析服务器)来将域名和IP地址相互映射，便于记忆


; 网络基础知识-端口
; 端口号(Port)是整数，取值范围在0-65535，它是用来标识计算机设备中的运行中的程序。

; 注意:HTTP协议默认端口号为 80，HTTPS协议默认端口为443


; 请描述IP、域名、端口的作用是什么?
; IP:唯一定位一台互联网上的设备  (127.0.0.1为本机地址).
; 域名:降低IP地址的书写与记忆成本  (localhost为本机域名)
; 端口:每一个程序启动运行后都会占用一个端口(0-65535)，用于标识进程


; 0SI网络模型:全球网络互连标准模型

; 应用层
; 表示层
; 会话层
; 传输层
; 网络层
; 数据链路层
; 物理层

; OSI七层网络模型


; TCP/IP网络模型:可以认为是0SI的简化版
; 应用层  HTTP、FTP、SMTP    （和用户交互，根据协议格式进行封装，数据传输，如HTTP请求、响应）
; 数据传输）

; 传输层(UDP、TCP)    (UDP:无连接、不保证数据传输的可靠性、速度快    应用在实时性要求高的场景（电脑屏幕共享、在线游戏）     
;                     TCP:面向连接、可靠、速度慢       应用在可靠性要求高的场景（文件传输、邮件）)
;                     负责将数据准确送到到对应的应用程序(端口)

; 网络层(IP)      负责基于IP地址将数据包路由给对应设备，实现不同网络之间的数据传输
;  负责数据包的路由，将数据包从源设备传输到目的设备

; 网络接口层      负责数据在物理网络中的传输，处理与硬件设备交互，如网卡、路由器等

; TCP/P四层网络模型


; HTTP协议
; 概念:Hyper Text Transfer Protocol,超文本传输协议，规定了客户端和服务器之间数据传输的规则。
; (只有在请求及响应中都遵循了统一的规则，服务端才能读懂客户端发送来的请求，客户端才能解析服务端响应的结果)

; 特点:
; 1.基于文本的协议:请求和响应的部分的协议内容为文本格式，底层通过TCP协议传输，稳定性强。
; 2.基于请求-响应模型:一次请求对应一次响应，必须由客户端先发起请求，服务端才会返回响应。
; 3.无状态:服务端不会记忆与客户端的历史交互信息，每次请求-响应都是独立的。
; 4.状态码:服务端返回给客户端的响应状态，表示请求是否成功，如200表示成功，404表示未找到，500表示服务器错误等。




; HTTP协议-请求数据格式
; POST /api/courses HTTP/1.1             请求行(请求方式、资源路径、协议)

; Accept: application/json, text/plain, */*Accept-Encoding: gzip, deflate, br, zstdAccept-Language: zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6
; Connection: keep-alive
; Content-Length: 139
; Content-Type: application/json
; Host: localhost:90
; Origin: http://localhost:90                                   #中间这一部分  请求头(格式 key:value)
; Referer: http://localhost:90/resource/course
; Sec-Fetch-Dest: empty
; Sec-Fetch-Mode: cors
; Sec-Fetch-Site: same-origin
; User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/143.0.0.0

; "phone":"18808088080","channel":1,"name":"虎","gender":1,"age":"32")   请求体(请求参数部分，GET方式没有，POST可以有)

; 请求方式:
; GET:请求参数在请求行中，没有请求体。如:/api/courses?name=Python&status=1。GET请求请求参数大小在浏览器中是有限制的。
; POST:请求参数在请求体中，POST请求大小是没有限制的。



; HTTP协议-响应数据格式
; HTTP/1.1 200                            响应行(协议、状态码)

; Server: nginx/1.24.0
; Date: Tue, 16 Dec 2025 12:38:08 GMT         #中间这一部分  响应头(格式 key:value)
; Content-Type: application/json
; Transfer-Encoding: chunked
; Connection: keep-alive

; {code:1,msg:"success", data: null}          响应体(响应数据部分)


; 200:客户端请求成功
; 400:请求参数错误
; 404:请求资源不存在，url输入有误，或者网站资源被删除了，或者请求的接口被关闭了
; 500:服务器发生了不可预期的错误




; 浏览器是一个客户端     get请求方式      但是调用大模型通常使用POST请求方式（因为POST请求方式可以携带更多数据）。




; Json格式
; JsoN(JavaScript Object Notation)是前端的一种对象表示方法。表示形式类似于Python中的字典，
; 都是key:value这种形式，不过所有的key都必须使用双引号引起来，值可以是任何类型:

; 对象:用{}表示，{}之间是键值对形式，键是字符串，值可以是任意其它类型
; 数字:整数和小数都是数字，例如:12、3.14
; 字符串:用""引起来，例如:"jack"
; 布尔:有两种值:true或false
; []中是列表的元素，多个元素以，分割。列表:用[]表示，[]之间是元素的集合
